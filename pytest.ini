[pytest]
testpaths = tests
pythonpath = .
; Live Sarvam-API canaries are deselected by default; run them explicitly
; with:  python -m pytest -m live -q
addopts = -m "not live"
markers =
    live: calls the real Sarvam API (costs credits; needs SARVAM_API_KEY)
